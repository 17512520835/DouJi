# V7.2 Map Phase2 Hotfix 候选说明

本候选只修第二阶段封箱阻断，不调整角色数值与卡牌文本。

## 修复
- `hexStep()` 去除旧 `cell.exists` 依赖，适配 MapState cell。
- `CHASE_WINDOW` 跨展开交接前建立新的权威 `expansion`，固定生命周期不变量。
- 浏览器资源点标记改读 `MapState.objectives.resource.cell`。
- 浏览器补给点只绘制 `MapState.objectives.supplies[].active` 当前项。

## 永久 Phase2 Regression
`v7.2_map_phase2_hotfix_regression.js` 固化 10 项：
hexStep、自动接近、自动后退、击退/强制位移/Occupancy、CHASE_WINDOW/expansion、缩域、危险区、补给、资源争夺、浏览器 UI 目标点同步。

## 实跑
- 旧 v720 对新专项：预期失败（复现有效）。
- v721 新专项：10/10 PASS。
- 原 Phase2 Regression：5/5 PASS。
- 法尤姆专项：5/5 PASS。
- Public API Fuzz：30 局 / 4307 commands / 0 failure。
- Headless Chromium 入口加载：环境级未确认（Core/UI静态同步测试已通过）。

## 第二阶段判定
自动化与 MapState/UI 同步检查已解除此前阻断，可重新进入 Golden Master 封箱候选；浏览器人工视觉/交互验收仍建议作为最终封箱前最后一道门。
