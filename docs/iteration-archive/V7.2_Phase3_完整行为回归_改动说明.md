# V7.2 Phase 3 完整行为回归改动说明

本次仅修改第三阶段测试与报告层，不改角色数值、Core 战斗逻辑、地图规则或 PassiveDrawSoftCap。

## 1. legacy version 容错改为显式 WARN

`v7.2_stage3_nine_role_signature_regression.js` 不再把子脚本 `exitCode=1` 重新改写为普通 `ok:true`。

现在每个 suite 输出：

- `status: PASS | WARN | FAIL`
- `functionalOk`
- `rawOk`
- `exitCode`
- `warnings[]`
- `failedAssertions[]`

仅以下明确登记的旧版本断言允许降级为 WARN：

- `V7.1 version`
- `版本与MapState公开查询`
- `MapState v2/坐标索引/邻接完整`

任何新失败名称或未登记失败均为 FAIL。

整套状态规则：

- 全部 PASS → `PASS`
- 至少一个 WARN 且无 FAIL → `PASS_WITH_WARNINGS`
- 任意 FAIL → `FAIL`

## 2. 新增第三阶段八类核心机制闭合回归

新增：`v7.2_stage3_core_mechanics_regression.js`

实际执行并确认：

- Skill：九角色各实际释放 1 个技能；
- Ultimate：九角色各实际结算 1 次绝技；
- Buff：实际打出“冠军怒吼”并验证 Buff 标记；
- Debuff：实际打出“尾羽震喝”并验证封技状态；
- ResponseWindow：复跑 1~4 击逐击响应窗口；
- 多段追击：复跑 1~4 击追击链；
- 反击：复跑第 2/3/4 击反击并确认无挂起窗口；
- PendingChoice：验证创建、提交关闭与私有信息隔离。

## 3. 本次实跑结果

`V7.2_Phase3_八类核心机制闭合_验证报告.json`：`PASS`。

八类覆盖均为 `true`，九角色 Skill 与 Ultimate 均完成实际执行。

`V7.2_Phase3_九角色完整行为回归_验证报告.json`：`PASS_WITH_WARNINGS`。

WARN 仅来自三个已登记的 legacy version assertion；功能回归全部通过。报告不再把这些 `exitCode=1` 伪装成普通 PASS。
